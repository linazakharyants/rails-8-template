<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.css">
<script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>

<style>
  .page-wrap {
    display: flex;
    gap: 18px;
    align-items: stretch;
    max-width: 1200px;
  }

  .calendar-col {
    flex: 1;
    min-width: 0;
  }

  .notes-col {
    width: 170px;
    display: flex;
  }

  .notes-card {
    height: 100%;
    border: 1px solid #ddd;
    border-radius: 12px;
    padding: 14px 14px 10px 14px;
    background: #fff;
    text-decoration: none;
    color: inherit;
    display: block;
    box-sizing: border-box;
  }

  .notes-title {
    font-weight: 700;
    font-size: 16px;
    margin-bottom: 10px;
  }

  .notebook-lines {
    border-radius: 10px;
    padding: 10px 10px;
    background:
      linear-gradient(#ffffff, #ffffff) padding-box,
      repeating-linear-gradient(
        to bottom,
        rgba(0,0,0,0.08),
        rgba(0,0,0,0.08) 1px,
        transparent 1px,
        transparent 22px
      );
    min-height: 320px;
  }

  .notes-hint {
    font-size: 12px;
    color: #666;
    margin-top: 10px;
  }

  .top-banner {
    width: 100%;
    margin: 10px 0 22px 0;
    padding: 12px 14px;
    border: 1px solid #ddd;
    border-radius: 16px;
    background: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
    box-sizing: border-box;
  }

  .top-banner a {
    padding: 8px 18px;
    border-radius: 999px;
    background: #f4f0ff;
    border: 1px solid #e2dbff;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    text-decoration: none;
    color: #3f3f46;
    transition: all 0.18s ease;
  }

  .top-banner a:hover {
    background: #ebe5ff;
    border-color: #d6ccff;
  }

  .top-banner a.active {
    background: #6b5cff;
    border-color: #6b5cff;
    color: white;
  }

  .highlight-pill {
    font-size: 12px;
    font-weight: 600;
    line-height: 1.25;
    padding: 4px 8px;
    border-radius: 8px;
    background: #ffd6e7;
    border: 1px solid #f2abc8;
    color: #7a0f3b;
    white-space: normal;
    word-break: break-word;
    overflow: hidden;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
  }

  .habit-pill {
    display: inline-block;
    margin-top: 6px;
    padding: 3px 8px;
    border-radius: 999px;
    background: #dff7e6;
    border: 1px solid #b8e7c5;
    color: #246b3c;
    font-size: 11px;
    font-weight: 700;
    line-height: 1.2;
    text-decoration: none;
    cursor: pointer;
  }

  .habit-pill:hover {
    background: #d4f1dd;
    text-decoration: none;
    color: #246b3c;
  }

  .frame-pill {
    display: inline-block;
    margin-top: 6px;
    padding: 3px 8px;
    border-radius: 999px;
    background: #dceeff;
    border: 1px solid #b9d8f8;
    color: #215a9a;
    font-size: 11px;
    font-weight: 700;
    line-height: 1.2;
    text-decoration: none;
    cursor: pointer;
  }

  .frame-pill:hover {
    background: #d2e8ff;
    text-decoration: none;
    color: #215a9a;
  }

  .frame-modal {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.55);
    display: none;
    align-items: center;
    justify-content: center;
    padding: 24px;
    z-index: 3000;
  }

  .frame-modal.open {
    display: flex;
  }

  .frame-modal-card {
    position: relative;
    display: inline-block;
    width: auto;
    max-width: 92vw;
    max-height: 90vh;
    background: #fffbfd;
    border: 1px solid #faeaf1;
    border-radius: 18px;
    box-shadow: 0 18px 50px rgba(0,0,0,0.2);
    overflow: hidden;
  }

  .frame-modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 16px 18px 12px 18px;
    background: #fffbfd;
  }

  .frame-modal-title {
    font-size: 18px;
    font-weight: 700;
    color: #8a1d4f;
    margin: 0;
  }

  .frame-modal-close {
    border: none;
    background: transparent;
    font-size: 28px;
    line-height: 1;
    cursor: pointer;
    color: #8a1d4f;
    padding: 0;
  }

  .frame-modal-image-wrap {
    display: flex;
    align-items: center;
    justify-content: center;
    background: transparent;
  }

  .frame-modal-image {
    display: block;
    width: auto;
    height: auto;
    max-width: 600px;
    max-height: 70vh;
    border-radius: 0;
    background: transparent;
  }

  @media (max-width: 1000px) {
    .page-wrap {
      flex-direction: column;
    }

    .notes-col {
      width: 100%;
    }

    .notebook-lines {
      min-height: 180px;
    }
  }
</style>

<h1>Calendar</h1>

<div class="page-wrap">
  <div class="calendar-col">

    <div class="top-banner">
      <a href="/highlights/<%= Date.today %>" id="nav-highlight">Line of the Day</a>
      <a href="/frames/<%= Date.today %>" id="nav-frame">Frame of the Day</a>
      <a href="/habits" id="nav-habits">Habit Tracker</a>
    </div>

    <div id="calendar"></div>
  </div>

  <div class="notes-col">
    <a class="notes-card" href="/notes">
      <div class="notes-title">Notes</div>
      <div class="notebook-lines"></div>
      <div class="notes-hint">Open notebook →</div>
    </a>
  </div>
</div>

<div id="frame-modal" class="frame-modal">
  <div class="frame-modal-card">
    <div class="frame-modal-header">
      <div class="frame-modal-title">Frame of the Day</div>
      <button type="button" id="frame-modal-close" class="frame-modal-close">&times;</button>
    </div>
    <div class="frame-modal-image-wrap">
      <img id="frame-modal-image" class="frame-modal-image" src="" alt="Frame of the Day">
    </div>
  </div>
</div>

<script>
document.addEventListener("DOMContentLoaded", function() {
  function escapeHtml(str) {
    return String(str || "")
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll('"', "&quot;")
      .replaceAll("'", "&#039;");
  }

  var path = window.location.pathname;

  if (path.includes("highlight")) {
    document.getElementById("nav-highlight")?.classList.add("active");
  } else if (path.includes("frame")) {
    document.getElementById("nav-frame")?.classList.add("active");
  } else if (path.includes("habit")) {
    document.getElementById("nav-habits")?.classList.add("active");
  }

  var modal = document.getElementById("frame-modal");
  var modalImage = document.getElementById("frame-modal-image");
  var modalClose = document.getElementById("frame-modal-close");

  function openFrameModal(photoUrl) {
    if (!photoUrl) return;
    modalImage.src = photoUrl;
    modal.classList.add("open");
  }

  function closeFrameModal() {
    modal.classList.remove("open");
    modalImage.src = "";
  }

  modalClose.addEventListener("click", closeFrameModal);

  modal.addEventListener("click", function(e) {
    if (e.target === modal) {
      closeFrameModal();
    }
  });

  document.addEventListener("keydown", function(e) {
    if (e.key === "Escape") {
      closeFrameModal();
    }
  });

  var calendarElement = document.getElementById("calendar");

  var calendar = new FullCalendar.Calendar(calendarElement, {
    initialView: "dayGridMonth",
    events: "/calendar.json",

    eventContent: function(info) {
      var title = info.event.title || "";
      return {
        html:
          "<div class='highlight-pill'>" +
          escapeHtml(title) +
          "</div>"
      };
    },

    eventDidMount: function(info) {
      var dayCell = info.el.closest(".fc-daygrid-day");
      if (!dayCell) return;

      var frameArea = dayCell.querySelector(".fc-daygrid-day-frame");
      if (!frameArea) return;

      var pct = info.event.extendedProps.habit_percent;

      if (pct !== null && pct !== undefined && !dayCell.querySelector(".habit-pill")) {
        var habitLink = document.createElement("a");
        habitLink.className = "habit-pill";
        habitLink.href = "/habits";
        habitLink.innerText = "Habits " + pct + "%";

        habitLink.addEventListener("click", function(e) {
          e.preventDefault();
          e.stopPropagation();
          window.location.href = habitLink.href;
        });

        frameArea.appendChild(habitLink);
      }

      var photo = info.event.extendedProps.photo;
      if (!photo) return;

      if (dayCell.querySelector(".frame-pill")) return;

      var link = document.createElement("a");
      link.href = "#";
      link.innerText = "Frame of the Day";
      link.className = "frame-pill";

      link.addEventListener("click", function(e) {
        e.preventDefault();
        e.stopPropagation();
        openFrameModal(photo);
      });

      frameArea.appendChild(link);
    },

    eventClick: function(info) {
      info.jsEvent.preventDefault();
      info.jsEvent.stopPropagation();
      window.location.href = "/highlights/" + info.event.startStr;
    }
  });

  calendar.render();
});
</script>
