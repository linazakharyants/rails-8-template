<div class="page-card">
  <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
    <h1 class="mb-0">
      <% if @the_day_entry.highlight_of_the_day.present? %>
        Update Today's Line
      <% else %>
        Add Today's Line
      <% end %>
    </h1>

    <div class="d-flex gap-2 flex-wrap">
      <a href="/" class="btn btn-outline-secondary rounded-pill px-3">Back To Calendar</a>
      <% if @the_day_entry.persisted? %>
    <a href="/delete_highlight/<%= @the_day_entry.id %>" class="btn btn-outline-danger rounded-pill px-3">Delete Line</a>
  <% end %>
    </div>
  </div>

  <div class="mb-4">
    <p class="text-muted mb-0"><strong>Date:</strong> <%= @the_day_entry.date %></p>
  </div>

  <hr class="my-4">

  <div>
    <h2 class="h4 mb-3">Line of the Day</h2>

    <form action="/upsert_highlight/<%= @date %>" method="post">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

      <div class="mb-3" style="max-width: 460px;">
        <label for="highlight_box" class="form-label fw-semibold">What moment will you remember today?</label>
        <input
          type="text"
          id="highlight_box"
          name="query_highlight_of_the_day"
          value="<%= @the_day_entry.highlight_of_the_day %>"
          class="form-control"
        >
      </div>

      <button type="submit" class="btn btn-dark rounded-3 px-4">
        <% if @the_day_entry.highlight_of_the_day.present? %>
          Update Line
        <% else %>
          Save Line
        <% end %>
      </button>
    </form>
  </div>
</div>
